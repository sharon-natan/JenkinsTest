// Define Variables
def gitCommit = None
def isImageCreated = False

pipeline {
    agent any
    stages {
        stage('Create new Image') {
            steps {
                sh 'ls -l'
            }
        }
    }

    post {
        
    }
}