// Define Variables
def gitCommit = ""
def isImageCreated = False

pipeline {
    agent any
    stages {
        stage('Create new Image') {
            steps {
                sh 'ls -l'
            }
        }
    }

    post {
        always {
            echo 'hi'
        }
    }
}