pipeline {
    agent any
    
    // Define Variables
    def gitCommit
    def isImageCreated = false

    stages {
        stage('Create new Image') {
            steps {
                sh 'ls -l'
            }
        }
    }
}